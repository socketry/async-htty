---
template: statement
duration: 10
transition: fade
---

HTTP/1.1 — one request at a time per connection.

# Translation

HTTP/1.1：1つの接続で1リクエストずつ。

---

In HTTP/1.1, a single TCP connection carries one request at a time. Send a request, wait for the response, send the next one.
