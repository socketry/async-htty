---
template: statement
duration: 11
transition: fade
---

One bootstrap. Then it's just HTTP/2.

# Translation

ブートストラップは1回だけ。あとはただのHTTP/2。

---

If one byte stream can carry many concurrent HTTP/2 requests, and the terminal *is* one byte stream — then we can run HTTP/2 right over the terminal.

Three pieces.
