---
template: statement
duration: 8
transition: fade
---

the receiver switches the session into raw byte mode

# Translation

受信側はセッションを生バイトモードに切り替える

---

The receiver sees the DCS, recognises it, and flips the session into raw byte mode. No more terminal interpretation.
