---
template: statement
duration: 11
transition: fade
---

from that point: plain `h2c` over stdin/stdout

# Translation

以降は stdin/stdout 上の素の `h2c`

---

And from that point on, it is just plaintext HTTP/2 — `h2c` — flowing over stdin and stdout. The framer at each end takes care of the rest.
