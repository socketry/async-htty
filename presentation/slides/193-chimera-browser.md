---
template: statement
duration: 12
transition: fade
---

pipes the bytes into an embedded HTTP/2 client + browser surface

# Translation

バイト列を内蔵HTTP/2クライアントとブラウザに流し込む

---

And then it pipes those bytes into an embedded HTTP/2 client, hooked up to an embedded browser surface. Suddenly the command process has a real browser as its UI.
